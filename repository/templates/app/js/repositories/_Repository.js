angular.module('<%= appname %>').factory('<%= name %>', ['$q', '$http',
    function($q, $http) {
        'use strict';

        return {

            get: function(url, qsParams, bodyParams, headers) {

            },

            post: function(url, qsParams, bodyParams, headers) {

            }

        };

    }]);
